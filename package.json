{
  "name": "uscg-incidents",
  "version": "1.0.0",
  "description": "USCG National Response Center incident analysis",
  "license": "MIT",
  "dependencies": {
    "@duckdb/node-api": "^1.3.2-alpha.26",
    "@observablehq/notebook-kit": "^1.4.1"
  },
  "scripts": {
    "preview": "notebooks preview --root docs --template template.html",
    "build": "notebooks build --root docs --template template.html -- docs/*.html"
  }
}
